[project]
name = "voidstrap-vuln-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "setuptools>=80",
    "undetected-chromedriver>=3.5.5",
]

[project.scripts]
collect-tokens = "game_leaderboard.collect_tokens:main"
vote = "game_leaderboard.vote:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/game_leaderboard"]
